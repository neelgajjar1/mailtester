{
  "name": "mailtester",
  "version": "1.0.0",
  "description": "Modern, high-performance email validation library with intelligent scoring, caching, and bulk validation",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "test": "yarn workspaces foreach -A run test",
    "lint": "yarn workspaces foreach -A run lint",
    "format": "yarn workspaces foreach -A run format",
    "typecheck": "yarn workspaces foreach -A run typecheck",
    "clean": "yarn workspaces foreach -A run clean",
    "docs:dev": "vitepress dev docs-site",
    "docs:build": "vitepress build docs-site",
    "docs:preview": "vitepress preview docs-site"
  },
  "keywords": [
    "email",
    "validation",
    "validator",
    "email-validation",
    "email-validator",
    "smtp",
    "dns",
    "mx",
    "disposable",
    "typo",
    "typescript"
  ],
  "author": {
    "name": "Muhammad Ali Kazmi",
    "url": "https://alikazmi.dev"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kazmiali/mailtester.git"
  },
  "bugs": {
    "url": "https://github.com/kazmiali/mailtester/issues"
  },
  "homepage": "https://github.com/kazmiali/mailtester#readme",
  "engines": {
    "node": ">=16.0.0",
    "yarn": ">=4.0.0"
  },
  "packageManager": "yarn@4.12.0",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@yarnpkg/pnpify": "^4.1.6",
    "eslint": "^9.39.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vitepress": "^1.6.4"
  }
}
